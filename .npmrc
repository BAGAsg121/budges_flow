# Keep the npm cache inside the workspace so installs work under a locked-down
# sandbox / restricted user profile (the default ~/.npm cache may be unwritable).
cache=./.npm-cache
audit=false
fund=false
